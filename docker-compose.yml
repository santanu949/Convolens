version: "3.8"

services:
  convolens:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - convolens_data:/app/data
    environment:
      - FLASK_ENV=production
      - PORT=5000

volumes:
  convolens_data:
